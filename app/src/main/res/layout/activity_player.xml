<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:fitsSystemWindows="true"
    android:theme="@style/ViewImageTheme">

    <com.google.android.exoplayer2.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/imageAudio"
        android:layout_width="@dimen/activity_player_iv_mic_size"
        android:layout_height="@dimen/activity_player_iv_mic_size"
        android:layout_gravity="center"
        android:alpha="0.5"
        android:src="@drawable/ic_mic"
        android:tint="@android:color/white"
        android:visibility="invisible" />

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorTransparentToolbar"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:title=" " />
</androidx.coordinatorlayout.widget.CoordinatorLayout>